---
name: Don't run tests
description: User has asked not to run tests automatically during implementation sessions
type: feedback
---

Don't run tests during implementation. The user will run them separately when ready.

**Why:** User explicitly requested not to run tests mid-session.

**How to apply:** Never invoke swift test, run-tests.sh, swift-test.sh, or any test runner during a coding session unless the user explicitly asks.
